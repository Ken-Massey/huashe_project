# 项目上下文（跨 Codex 对话）

> 最后核对：2026-09-09。每次新开 Codex 对话先阅读本文件，再执行 `git status --short`、`git diff` 并检查最近提交；本文件只记录可由代码、Git 和项目文档证实的当前状态。

## 项目简介

“华设轨道智审系统”是面向城市轨道交通保护区的综合管理系统。它将项目档案、智能审核/复函意见、知识库、巡查任务与现场符合性核查整合在一起。

## 当前主要目标

- 近期已落地：完善本地南京地图、地址候选和项目自动定位；将终审意见按项目汇入开工前现场核查任务。
- 当前优先级：提升 IMA-RAG 的审核依据可追溯性。已完成知识库主规程优先、外部规范受控补充；优先以“《规程名称》第 X 条”形成意见，未命中规程时可明确标记为“项目文件依据”或“待人工复核”，不得伪造条文。巡查 P1 剩余的“锁定现场原始隐患描述”仍待处理。完整产品排期及验收状态：**待确认**。

## 当前 Git 状态

- 分支：`main`，跟踪 `origin/main`。
- 当前未提交改动：`PROJECT_CONTEXT.md`，以及外部规范受控入库/RAG 回退所涉及的 Python、Java、Vue 和测试文件；未暂存。不得回滚这些改动。
- 最近提交：`789d6cb`（2026-09-08，完善本地地图定位与地址候选）。其改动包括审核页地址候选/定位、文档信息抽取补项目地点、Python 本地地理编码 CORS 和本地地图 Compose 配置。
- 紧邻的巡查提交（2026-09-07）实现了项目—巡查一对一、按项目汇总审核意见、多线路/多巡查员、意见现场核查及 Word 导出。

## 技术栈

- 管理端：RuoYi-Vue 3.9.2，Vue 2.6、Element UI、Axios、ECharts、MapLibre GL；目录 `RuoYi-Vue-master/ruoyi-ui`。
- 后端代理：RuoYi Java/Spring Boot、MyBatis、Maven；目录 `RuoYi-Vue-master`。浏览器应访问 Java，不直接访问 Python。
- 智能服务：Python/FastAPI/Uvicorn；目录 `audit_api`。使用 SQLite 存储运行期业务数据和任务状态。
- 智能审核：LLM + 向量 RAG（`BAAI/bge-m3`）、MinerU 文档解析（可选云端）、DeepKE 相关案例处理脚本、第一阶段规则计算与 Word 输出。
- 巡查移动端：微信小程序，目录 `patrol_miniprogram`。
- 本地地图：Docker Compose 部署 Nominatim + TileServer GL；南京 OSM PBF / MBTiles 数据不进 Git。

## 核心目录与模块

| 路径 | 用途 |
|---|---|
| `audit_api/` | FastAPI 主服务：智能审核、项目档案、知识库、审查会话、智能体、巡查和本地地理编码。入口为 `main.py`，巡查为 `patrol.py`。 |
| `RuoYi-Vue-master/` | Java 代理/权限/菜单和 Vue 管理端；轨道业务控制器位于 `ruoyi-admin/.../controller/rail/`。 |
| `patrol_miniprogram/` | 巡查员小程序：登录、任务、上传、隐患页面。 |
| `stage1_reply_system/` | 第一阶段函件抽取、规则评估、历史回函匹配、复函/审查材料生成。 |
| `deepke_case_extract/` | 案例文本/结构化属性/评审建议提取和相似案例匹配脚本。 |
| `local-map/` | 南京离线地图和地址检索部署文件。 |
| `RuoYi-Vue-master/sql/` | 若依菜单、权限、审核流程、通用管理等 MySQL 初始化/修复脚本。 |
| `现场符合性巡查模块_问题清单总览.md` | 巡查模块已修复项、待办、风险及已确认边界的权威清单。 |
| `patrol_miniprogram/设计决策记录.md` | 巡查业务设计决策；实现前应先核对相关决策编号。 |

## 已完成功能

- 第一阶段：函件抽取、人工参数合并、退让距离/影响等级/安全评估/保护监测、历史回函匹配和 Word 草稿。
- 第二阶段：案例全文解析、案例与技术规程的语义检索、LLM 证据对照审核、自动审核意见、相似案例建议和结果文件。
- 知识库：案例与技术规程的上传、解析、检索、停用/恢复、文件下载和统计；有效案例参与建议匹配。
- 项目档案：项目、阶段、审核记录、历史上下文和源文件归档；支持重跑已成功审核。
- 审核会话/智能体：审核意见项和对话记录持久化，支持基于案例/规程知识检索。
- 巡查：任务派发、记录/媒体、隐患五态闭环、监测方案附件、任务编辑日志、多线路/多巡查员。
- 终审意见现场核查：同一项目唯一巡查任务，项目档案审核意见同步为意见项；小程序按意见上传照片，平台核查通过/退回并可导出项目级 Word。
- 本地地图与地址候选：项目地点从文档识别，优先经 Java 代理调用本地地理编码；当前运行中 Java 代理会返回空数组时，Vue 回退直连仅监听本机的 Python 地理编码服务。坐标约定为 WGS84。
- 收函单位规范化：资料识别、草稿恢复、人工修改和提交载荷均去除“委托单位：”“建设单位：”等标签前缀，仅保留单位名称。
- 地铁区间规范化：地铁线路与区间分列保存；仅当资料中出现明确的“起始站～终止站”站名对时才填写区间字段，去除重复的“X 号线”和“区间”字样；其他含“隧道/区间”的泛文本必须留空。
- 项目位置与邻近审核：`location` 只保存可地理编码的具体项目落点并据此保存坐标；识别顺序为项目名称 → 原始文件名 → 正文地点字段。“西侧紧邻某路”等方位/周边关系描述不得写入。后续仅以坐标计算 50 米邻近项目，并将邻近项目作为综合审核的上下文。

## 当前正在做的功能

- 巡查 P1 已完成其中四项：隐患截图上传和媒体/截图下载已有任务行级隔离，非待确认隐患不可删除，确认人/时间已落库；剩余原始隐患描述字段保护。
- 本地地图服务已接入代码和 Compose 配置；南京 PBF/MBTiles 数据、部署环境是否已实际导入并验证：**待确认**。

## 尚未完成的任务

按已记录优先级执行，完成后应更新本节并移入“已完成功能”。

1. P1：锁定现场原始 `description`，不允许平台修改；仅允许平台调整隐患类型、风险等级、整改责任人和整改要求。需要补充回归测试。P1 #4/#5/#6/#8 已由当前代码实现，但问题清单尚未更新。
2. P2：任务检查结论；按筛选导出台账；完整任务改派/状态留痕；整改反馈嵌套到对应隐患；小程序任务分页；Web 历史任务按后端 `legacy` 查询。媒体水印已包含上报人，原问题清单中对应 #12 的状态同样过期。
3. P3/技术债：弱网 GPS 降级、图片加载优化、软删除子数据治理、字典写接口鉴权、字典缓存刷新、清理未注册旧页面、移除隐患冗余列、处理重复用户名 `xcy`。
4. 协作治理：将案例建议数据库的绝对路径改为相对路径或停止跟踪生成物；补充运行时 SQLite/上传文件等忽略规则（当前 `.gitignore` 已忽略本地地图数据和日志，但未见 `audit_api/runtime/` 的忽略规则）。

## 已确定的重要设计方案

- 架构边界：Vue → RuoYi Java（鉴权、权限、业务记录、文件代理）→ Python；不得让浏览器直接暴露 Python 地址或服务令牌。
- 正式智能审核使用 LLM + RAG；旧动态规则和 `chapter_1_functions` 保留用于历史兼容/回归对比，不参与当前正式审核结论。
- RAG 切分不使用 token 阈值：待审案例按原始段落合并为约 850 个字符的块，跨块保留前 2 段重叠；技术规程优先“一条编号条文一个块”，无法识别条文时按约 900 个字符、前 1 段重叠兜底。单个超长段落不会被强行截断，因此可能超过该字符目标。
- 正式 IMA-RAG 审核的向量检索语料是“本次待审文件 + 已启用技术规程”，按审核维度分别检索双方证据后由 LLM 判断；案例知识库不作为该链路的第二个向量语料库，而用于知识库检索、智能体问答和历史案例建议匹配。
- RAG v10 检索链路：块级 Metadata（来源、文件、页码、段落/条文号、内容类型、规程知识类型、来源层级/核验状态）→ 可靠来源硬过滤、工程类型软加权 → 向量 + 本地 BM25 + 精确术语混合召回（Top 30）→ 无外部依赖的确定性本地重排 → 主规程优先/受控外部规范回退 → LLM 双侧证据审核。模型必须原样返回审核维度标题；若遗漏规程块 ID，系统仅从该维度实际检索到的条款中自动绑定依据，绝不编造条文。数值/限值类维度会偏好定量和表格条文；Metadata 缺失的工程属性不作硬过滤。
- 审核依据来源层级已确认并实现第一版：绝大多数审核意见优先使用已入库、启用的主规程；仅当主规程检索未达到最低相关性时，才查询外部补充规范。外部规范必须通过现有上传链路归档原文件，记录版本/标准编号、发布机构、生效信息、稳定来源链接、检索日期（可选备注），初始为停用和 `pending_verification`；管理员确认来源核验后才可启用并参与审核。服务端不会直接抓取任意 URL。未固化、不可追溯网页内容只能作为“待人工核验线索”，不得形成正式审核结论。
- 正式审核意见的用户界面规则：审核依据自动显示知识库中的“《规程文件名》第 X.X.X 条”；不要求用户额外填写引用依据，也不重复展示“引用依据”区块。RAG 找不到同时具备规程文件名和条款号的证据时，该风险信号不得生成正式审核意见。
- 审核兜底规则：正常目标为 5–6 条分项意见，绝不靠编造内容凑数。无本地规程条款但有待审项目原文定位时，保留为“项目文件依据”，只能提出补充、核验、落实要求，不作为规程符合性结论；连项目原文定位也没有时，标记“待人工复核”。页面以标签区分“规程依据 / 项目文件依据 / 待人工复核”。自动联网检索尚未接入，原因是当前没有受控的检索服务配置；不得以普通网页摘要冒充规程条款。
- 知识库案例只有 `ready` 状态才参与匹配；停用保留数据/原件但退出检索，恢复后重新参与。
- 项目档案阶段审核记录一阶段一条（`audit_records.stage_id` 唯一）；审核来源文件采用内容哈希归档。
- 巡查权限分工：平台端负责任务、字典和审核；小程序巡查员只执行任务。小程序不做字典管理、不允许改任务、不做持续轨迹或现场点位打卡；上传时记录单点 GPS。
- 巡查任务按项目唯一：终审通过后首次建任务；同项目后续阶段只追加意见。意见从项目档案 `audit_records` 的审核意见汇总。
- 意见项状态机：`pending_photo` → `photo_taken` → 平台通过 `done`；退回为 `returned` 并可重传。每意见最多 9 张照片。
- 现场原始隐患描述属于证据链；后续实现必须遵守 P1 的字段边界和状态限制。
- 地图/地理编码仅使用内网本地服务；正常链路为 Vue → Java 代理 → Python，本机代理异常或空结果时才使用本机 Python 回退；坐标持久化统一 WGS84，历史 GCJ-02 数据迁移前必须转换。

## 数据库重要表及用途

> 运行期 SQLite 默认在 `audit_api/runtime/`，路径由 `audit_api/config.py` 集中配置；表由代码自动建表/迁移。若需变更表结构，必须同时提供存量库迁移与回归验证。

| 数据库/表 | 用途 |
|---|---|
| `project_archive.sqlite3`: `projects`、`project_stages`、`audit_records` | 项目、阶段及每阶段智能审核档案、结果 JSON、源文件/产物引用。 |
| `audit_sessions.sqlite3`: `audit_sessions`、`audit_review_items`、`audit_chat_messages` | 审核会话、可人工调整的审核意见项和对话版本记录。 |
| `patrol.sqlite3`: `patrol_tasks` | 巡查主任务；含项目/流程来源、指派、状态、监测字段。`source_project_id` 对未删除任务唯一。 |
| `patrol.sqlite3`: `patrol_records`、`patrol_media` | 日常/整改记录及其照片、视频。 |
| `patrol.sqlite3`: `patrol_hazards`、`patrol_hazard_shots` | 隐患五态闭环及标注截图。 |
| `patrol.sqlite3`: `patrol_task_opinions`、`patrol_opinion_photos` | 终审意见的现场核查项及逐项照片；意见在任务/项目/阶段/序号维度去重。 |
| `patrol.sqlite3`: `patrol_task_docs`、`patrol_task_lines`、`patrol_task_users`、`patrol_task_edit_logs`、`patrol_dict` | 监测/任务附件、多线路、多巡查员、编辑摘要和业务字典。 |
| `knowledge.sqlite3`: `kb_case` 等 | 案例知识库、文件属性与可检索内容。 |
| `regulations.sqlite3`: `regulation`、`regulation_clause`、`regulation_rule` 等 | 技术规程、条款、生成规则及文件夹。`regulation` 已包含 `source_tier`、发布机构/链接/日期、核验状态与备注；外部补充规范未核验不可启用。 |

## 关键接口或路由

- Python 健康检查：`GET /health`。
- Python 智能任务：`POST /api/v1/stage1/tasks`、`/api/v1/stage2/audit/tasks`、`/advice/tasks`、`/full/tasks`；查询 `GET /api/v1/tasks/{task_id}` 及结果/文件子路由。
- Python 项目档案：`/api/v1/project-archives/projects`、`/projects/nearby`、`/resolve`、阶段和审核记录子路由。
- Python 知识库：`/api/v1/knowledge/cases`、`/regulations`、`/assets`、`/rules`、`/stats`；外部规范来源核验为 `POST /api/v1/knowledge/regulations/{id}/source-verification`；均应由 Java 代理并携带服务令牌。
- Python 本地地图：`GET /api/v1/local-geocoder/status`、`/search`。
- Python 巡查前缀：`/api/v1/patrol`；任务 CRUD/状态/记录/隐患/媒体/文档，以及意见 `GET /tasks/{id}/opinions`、同步、照片上传、提交、复核、核查、`POST /export/workflow-word`。
- 管理端主要页面：`src/views/rail/audit/index.vue`、`archive/index.vue`、`patrol/index.vue`、`knowledge/index.vue`、`workflow/index.vue`、`agent/index.vue`。实际 Java 对外路由以 `Rail*Controller.java` 当前注解为准，改动时前后端同步核对。

## 当前已知 Bug / 报错 / 风险

- **已确认阻塞：** `audit_api/requirements.txt` 含未解决的 Git 冲突标记（`<<<<<<<`、`=======`、`>>>>>>>`）。在此状态下依赖安装会失败，修复后应验证依赖安装和 Python 测试。
- **已确认安全/完整性待修复：**巡查 P1 仅剩 #7（平台可改写现场原始 `description`）；P1 #4/#5/#6/#8 已在当前代码实现，P0 #1–#3 也已修复。巡查问题清单的 P1 状态滞后，后续应同步更新。
- **已确认配置风险：** `local-map/docker-compose.yml` 当前包含明文 `NOMINATIM_PASSWORD: 123456`。内网部署前应改为外部私密配置；是否已暴露到生产环境：**待确认**。
- **已确认协作风险：** `deepke_case_extract/data/case_advice_database.json` 被 Git 跟踪且含构建机绝对路径；运行时数据/上传产物的忽略范围不完整。
- **已确认审核可解释性缺陷：** IMA-RAG 在仅有案例原文、没有 `regulation_evidence` 时会把“符合/不符合”降级为工程风险判断，但仍保留该条并可被 AI 润色为普通审核意见；页面未将“无规程依据的待复核项”与“有双侧证据的正式审核结论”强区分。截图中“未找到可引用的规程条款或资料定位”即为该路径，应优先修复展示和发布门禁。
- **已确认待调优：**主规程是否“无适用依据”当前由本地重排分数阈值 `0.34` 判定，尚未用真实规程/案例评测集标定；阈值、外部规范适用范围和人工核验职责应在上线前确认。
- **已修复、待端到端复验：**地址候选曾因 Vue 根据环境变量直连 `127.0.0.1:8000`，且 Python CORS 正则转义错误而静默返回空数组；现已修正 CORS，代理查询权限也从单一 `rail:audit:run` 放宽为审核相关任一既有权限。运行中 Java 代理仍对已验证的“河海大学”回传空数组，因此 Vue 现采用“Java 代理优先；空结果或失败时本机 Python 回退”，前端请求失败时明确显示服务不可用。已在 `8001` 启动加载新 CORS 的临时本机地理编码服务：`Origin http://127.0.0.1:80` 对“南京 河海大学”返回 2 条候选。后续应查明 Java 运行时代理空数组的根因，并在服务重启后收敛为单一 `8000` 服务。
- **测试环境已知问题：**完整 `audit_api/tests/test_api.py` 当前有两项失败，均与本次 RAG 无直接关联：`test_legacy_stage1_task_also_only_exposes_two_public_files` 在 Windows 下未匹配其乱码文件名 fixture；`test_session_messages_are_persisted_and_user_scoped` 清理临时 SQLite 时遭遇文件占用。新增的规程条文门禁定向测试与 IMA-RAG/规程库回归均通过。
- **待确认：**未执行完整测试、Java/Maven 构建、Vue 构建、Docker 地图启动或真实端到端验收；不要将“代码存在”视为“已部署可用”。

## 不能随意修改或回滚的内容

- 不要回滚或绕过“项目唯一巡查任务 + 终审意见追加”模型、意见项状态机、项目档案作为意见来源的设计；它们对应巡查决策 D95–D103 和近期提交。
- 不要让前端直连 Python、暴露 `AUDIT_API_TOKEN`/MinerU 密钥，或把密钥提交到源码。
- 不要把线上运行时 SQLite、上传文件、地图 PBF/MBTiles、个人绝对路径当作可共享源码提交；删除巡查/档案数据前须确认数据保留和迁移策略。
- 对 `patrol_tasks`、`patrol_hazards`、`patrol_task_opinions` 等自动迁移表，禁止只改新库 `CREATE TABLE` 而遗漏已有 SQLite 的 `ALTER`/索引兼容。
- 不要把 WGS84 和 GCJ-02 坐标混用。

## 下一步建议

1. 先修复并验证 `audit_api/requirements.txt` 的冲突标记，再运行与改动模块相关的 Python 测试。
2. 在真实“主规程命中/主规程缺失”的样本上校准 RAG 回退阈值；上传一份受控外部规范，依次验证待核验→核验→启用→可引用的完整流程。
3. 修复审核页面和发布门禁：没有规程条款的工程风险提示不得作为正式合规/不合规结论展示或入档。
4. 完成巡查 P1 #7，并补充平台/巡查员权限与状态回归测试；随后同步巡查问题清单的已完成状态。

## 维护规则

- 每完成重要功能、调整上述设计、解决关键 Bug、新增重要表或接口，**直接改写相关小节**使本文件保持现状；不要追加流水账。
- 信息无法由仓库、Git、代码、测试或用户确认得出时，写“待确认”，不要猜测。
