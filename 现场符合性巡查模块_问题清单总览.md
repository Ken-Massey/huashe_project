# 现场符合性巡查模块 — 问题清单总览

> 整理日期：2026-08-27
> 范围：微信小程序 + Web端 + FastAPI后端 + 若依Java代理
> 来源：功能逻辑审查 + 增删改查比对 + 用户自提缺陷 + 协同开发审查

---

## 一、P0 — 安全漏洞（已修复 ✅）

| # | 问题 | 修改理由 | 修复方案 | 状态 |
|---|------|----------|----------|------|
| 1 | **媒体上传无行级隔离** | `addMedia` 未传 actorHeaders，后端 `add_media` 无 actor 校验，巡查员可给他人任务的记录上传照片/视频，污染他人巡查数据 | Java 层 `addMedia()` 传入 `actorHeaders()`；Python 层 `add_media` 路由加 `actor: Depends(get_actor)`，上传前通过 `repo.get_task(record["task_id"], actor)` 校验任务可见性 | ✅ 已修复 |
| 2 | **`get_actor()` 空 user_id 默认管理员** | `is_admin = ... or not user_id`，缺失身份头时直接获得管理员权限，可看全部任务；若 FastAPI 直连暴露则越权 | 移除 `or not user_id`，仅当 `X-Actor-Is-Admin=true` 时才是管理员 | ✅ 已修复 |
| 3 | **`list_tasks`/`statistics` 空 user_id 匹配历史空指派任务** | v1 迁移的历史任务 `assigned_user_id=''`，空身份的 `user_id=''` 恰好匹配，导致空身份能看到2个历史任务 | 非管理员且 `user_id` 为空时追加 `1=0` 永假条件，不参与查询 | ✅ 已修复 |

---

## 二、P1 — 安全/数据完整性（建议本轮修复）

| # | 问题 | 修改理由 | 建议方案 | 状态 |
|---|------|----------|----------|------|
| 4 | **隐患截图上传无行级隔离** | `addHazardShot` 未传 actorHeaders，后端无 actor 校验，巡查员可给他人任务的隐患上传圈注截图 | 与 #1 同方案：Java 加 actorHeaders + Python 加 actor 校验 hazard→task 可见性 | ⏳ 待修复 |
| 5 | **媒体/截图下载无行级隔离** | `mediaFile`/`shotFile` 只有 VIEW_PERMS 权限校验，无行级隔离；登录用户知道 media_id 即可下载他人照片（含地铁敏感设施信息） | 下载接口加 actor，查 media→record→task→assigned_user_id 比对；或改用签名 URL | ⏳ 待修复 |
| 6 | **Web端隐患删除无状态限制** | admin 可删除任意状态隐患，进入整改流程后删除会导致整改记录成为孤儿数据、证据链断裂 | 限制仅 `pending_confirm` 状态可删；`pending_rectify` 及以后不可删，只能通过闭环流程处理 | ⏳ 待修复 |
| 7 | **Web端可修改隐患描述** | 隐患描述是巡查员现场原始证据，平台修改破坏证据链和审计追溯 | `update_hazard` 对 admin 也锁定 `description` 字段，只允许改 `hazard_type`/`risk_level`/`rectify_owner`/`rectify_requirement` | ⏳ 待修复 |
| 8 | **隐患确认人/确认时间未记录** | 五态流转中"待确认→待整改"缺操作人/时间记录，审计追溯不完整；`review_hazard` 有 reviewer/review_time，但 `confirm_hazard` 没有 | hazard 表加 `confirmer`/`confirm_time` 列，`confirm_hazard` 时写入 | ⏳ 待修复 |

---

## 三、P2 — 功能缺失（建议下一轮迭代）

| # | 问题 | 修改理由 | 建议方案 | 状态 |
|---|------|----------|----------|------|
| 9 | **任务级"检查结论"字段缺失** | 需求规格明确要求"系统自动记录巡查台账、巡查轨迹、**检查结论**"；当前任务只有四态（待执行/执行中/已完成/已关闭），无符合性结论（符合/不符合/部分符合） | 任务表加 `conclusion`（符合/不符合/部分符合）+ `conclusion_desc`（结论描述）+ `concluded_by`/`concluded_at`；平台标记完成时强制填写 | ⏳ 待修复 |
| 10 | **台账/报表导出缺失** | 需求规格要求"自动生成报表""批量导出"；当前只有 `GET /statistics` 统计数字，无 Excel/PDF 导出，Web端无导出按钮 | 加 `GET /tasks/export`（Excel），支持按筛选条件导出巡查台账（任务+记录+隐患+媒体链接）；Web端加导出按钮 | ⏳ 待修复 |
| 11 | **任务修改/改派不留痕** | D29 明确要求"每次修改留痕"；需求4.2要求"所有业务操作全程留痕"；当前 `update_task` 直接覆盖，改派后无法追溯"谁在什么时候把任务从张三改派给了李四" | 新建 `patrol_task_change_log` 表，记录 task_id/field/old_value/new_value/changed_by/changed_at；`update_task` 和 `set_task_status` 中写入 | ⏳ 待修复 |
| 12 | **水印缺"上报人"信息** | D3 明确要求水印含"时间/定位/上报人"；当前水印只有任务编号/时间/线路/位置，缺拍摄者（巡查员姓名/账号）；作为违规施工证据链，"谁拍的"是关键溯源信息 | `add_media` 已有 actor，水印 lines 加 `f"上报人 {actor['name']}"` | ⏳ 待修复 |
| 13 | **整改反馈未嵌套在隐患下方**（用户自提问题1） | 当前整改记录（type=rectify）和日常巡查记录平铺在同一条时间线，不嵌套在对应隐患下方，看不出"哪条整改对应哪个隐患"；用户希望隐患旁加"解决"按钮，上传截图+说明，内容接在隐患下方 | 后端能力已具备（rectify记录+hazard_id关联），主要是前端调整：小程序/Web端时间线改为隐患块下方展示关联的rectify记录；隐患旁加"整改"按钮跳上传页（预填hazard_id和rectify类型） | ⏳ 待修复 |
| 14 | **小程序任务列表无分页** | 固定加载50条（`size: 50`），历史任务超过50条无法访问；后端已支持分页 | 加上拉加载更多，分页请求 | ⏳ 待修复 |
| 15 | **Web端历史任务加载上限200条** | 前端拉全部任务（最多200条）再前端过滤 `legacy=1`，活跃任务多或历史任务超200条时会丢失 | 后端 `list_tasks` 加 `legacy` 查询参数；前端按Tab分别请求 | ⏳ 待修复 |

---

## 四、P3 — 体验/性能/技术债（可排后）

| # | 问题 | 修改理由 | 建议方案 | 状态 |
|---|------|----------|----------|------|
| 16 | 小程序任务详情串行下载所有照片 | 嵌套 for+await 串行下载，一个任务3条记录×9张=27次串行HTTP请求，弱网下等待几十秒 | 改并行下载（Promise.all分批）或懒加载（点击才加载原图） | ⏳ 待修复 |
| 17 | 强制GPS导致地下/弱网场景无法上传 | 地铁隧道口GPS信号弱，定位失败就不能提交巡查记录 | 定位失败时允许手动填写位置描述后提交（坐标可空但备注必填） | ⏳ 待修复 |
| 18 | 软删除任务的子数据成为孤儿 | `soft_delete_task` 只设 deleted=1，不级联删除子表；被删任务下的 records/hazards/media 仍在数据库 | 加定时清理任务，或软删除时级联标记子数据 deleted=1 | ⏳ 待修复 |
| 19 | 字典操作后端缺 is_admin 校验 | 依赖Java层 @PreAuthorize，FastAPI直连时可越权增删改字典 | 字典写路由加 actor 并校验 is_admin | ⏳ 待修复 |
| 20 | 小程序字典缓存不自动更新 | 登录时缓存字典到 localStorage，Web端改了字典后小程序需重新登录才同步 | 启动时比对字典版本号，或加下拉刷新字典 | ⏳ 待修复 |
| 21 | `pages/detail/` 旧版残留 | v1自由上报时代页面，引用已废弃的 `/api/v1/patrol/events/` 接口和 `downloadPhoto` 函数；app.json 未注册 | 删除该目录 | ⏳ 待修复 |
| 22 | hazard表 `media_id`/`video_time` 冗余字段 | D66 已决定"移除 media_id/video_time 定位，靠隐患截图定位"，但表结构仍保留这两列且永远为空字符串 | 数据库迁移时 DROP COLUMN（SQLite 3.35+支持） | ⏳ 待修复 |
| 23 | 数据库重复用户名 xcy | user_id=4 和 user_id=5 都叫 xcy，可能导致登录歧义 | 清理 user_id=4（或重命名） | ⏳ 待修复 |

---

## 五、协同开发问题

| # | 问题 | 修改理由 | 建议方案 | 状态 |
|---|------|----------|----------|------|
| 24 | **`case_advice_database.json` 硬编码绝对路径被Git跟踪** | JSON中 `source_root` 和每条advice的 `source_file` 都是构建者本地绝对路径（如 `D:\develop\huashe\...`）；文件被Git跟踪，提交后覆盖别人路径，产生无意义diff冲突，泄露本地目录结构 | **功能上不影响**（无代码读取source_root），但协作上有问题；建议：①不要提交该文件的路径变更；②把 `deepke_case_extract/data/` 加入 .gitignore；③或构建脚本改为写相对路径 | ⏳ 待修复 |
| 25 | **`.gitignore` 未忽略 `runtime/` 和构建产物** | 当前 .gitignore 只忽略了3个若依配置文件；`audit_api/runtime/`（SQLite数据库、上传照片视频、水印文件）和 `deepke_case_extract/data/`（构建产物）都可能被误提交 | .gitignore 追加：`audit_api/runtime/`、`deepke_case_extract/data/`、`*.sqlite3`、`*.sqlite3-journal` | ⏳ 待修复 |

---

## 六、用户自提问题分析结论

| 用户问题 | 结论 | 对应条目 |
|----------|------|----------|
| 1. 隐患旁加"解决"按钮，整改截图/说明嵌套在隐患下方 | **有必要**，属于交互/展示优化，后端能力已具备，主要是前端调整 | #13 |
| 2. Web端是否有必要具备隐患增/改权限 | **有必要保留，但必须加状态约束和字段边界**：增添有必要（平台可能发现漏报隐患）；修改有必要但应锁定description；删除应限制仅pending_confirm可删 | #6 #7 |
| 3. 小程序端是否需要字典管理/任务修改 | **不需要**：巡查员只需选择字典值，无权修改系统配置；任务由平台派发，巡查员无权修改线路/位置/要求；保持职责分离 | 不做 |

---

## 七、不做（确认排除）

| 功能 | 排除理由 |
|------|----------|
| 小程序端字典管理 | 巡查员只需选择字典值，不需要增删改；职责分离 |
| 小程序端修改任务信息 | 任务由平台派发，巡查员是执行者，无权修改线路/位置/要求 |
| 现场点位打卡 | D37 明确"本轮不做打卡，上传时记录GPS即可" |
| 巡查轨迹（连续定位追踪） | D37 明确简化为上传时单点GPS |

---

## 八、建议执行顺序

1. **本轮（P1）**：#4–#8，共5项，安全/数据完整性问题，每项约10–30行代码，#4修复方式与已修复的P0-#1完全一致
2. **下一轮（P2）**：#9–#15，共7项，涉及新字段/新接口/前端交互调整，工作量中等
3. **后续（P3）**：#16–#23，体验优化和技术债，不影响核心功能
4. **协同开发**：#24–#25，建议立即处理 .gitignore，避免后续误提交
